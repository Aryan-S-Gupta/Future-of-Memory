import { useNavigate } from "react-router-dom";
import BasePage from "./BasePage.jsx";
import Button from "../components/Button/Button.jsx";

/**
 * HowToScreen component
 *
 * This screen provides players with instructions on how to play the game.
 * It introduces the gameplay loop:
 *  - Reading scenarios
 *  - Making ethical/scientific decisions
 *  - Seeing the cumulative impact of choices
 *
 * Features:
 * - Displays structured instructions with headings and paragraphs.
 * - Uses `BasePage` for consistent layout and styling.
 * - Provides a "Back" button to return to the home screen.
 *
 * @component
 * @returns {JSX.Element} The instructional screen with game rules and navigation.
 */
const HowToScreen = () => {
  // Hook for navigation between routes
  const navigate = useNavigate();

  return (
    <BasePage>
      <h1 className="title">Instructions</h1>
      <div className="text-container">
        <p>
          Welcome! to <strong>The Future Of Memories</strong>. In this interactive experience,
          you will navigate a world shaped by your decisions on neurotechnology and ethics.
        </p>

        {/* Step 1: Scenario */}
        <div className="subheading">Read the Scenario</div>
        <p>
          Each level begins with a short narrative scenario generated by advanced AI systems.
          These reflect real-world neuroscience concepts and speculative futures.
        </p>

        {/* Step 2: Decision */}
        <div className="subheading">Make Your Decision</div>
        <p>
          After the scenario, you’ll be asked a key ethical or scientific question. 
          Your responses influence how neurotechnology advances and is applied in the world.
        </p>

        {/* Step 3: Impact */}
        <div className="subheading">See the Impact</div>
        <p>
          At the end of your journey, an AI-generated artwork visualizes the world you shaped, 
          showing the cumulative effects of your decisions.
        </p>
     
        {/* Navigation Button */}
        <div className="button-group">
          <Button baseButton="btn-back" action={() => navigate("/")} title="Back" />
        </div>
      </div>
    </BasePage>
  );
};

export default HowToScreen;
